config:
  - group: !include_dir_named "dotbot"

profiles:
  common: &common
    - devtools
    - fonts
  macos:
    - *common
    - containertools
    - arc
  linux: &linux
    - *common
    -
  ubuntu:
    - *linux
    -

default_profile:
  - macos: '{{ eq OS "darwin" }}'
  - ubuntu: '{{ MatchDistro "ubuntu" }}'
  - linux
